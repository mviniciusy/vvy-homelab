# Timelog — 2026-09-15

## Servidor dedicado Project Zomboid na VM Oracle (B42.20.4, ARM via FEX-Emu)

**O que foi feito**
- Instalado servidor Project Zomboid Dedicated Server Build 42.20.4 na VM Oracle (Arm Ampere).
- Caminho final: container Docker `etheth888/project-zomboid-arm64` (FEX-Emu embutido), com binarios x86_64 obtidos via SteamCMD logado no PC (o app dedicado 380870 nao baixa mais anonimo) e transferidos por rsync vvy -> VM via Tailscale.
- Recursos: `--cpus 4 --memory 16g`, heap JVM `-Xmx12g`, servidor `pzvvy`, mundo gerado e funcional.
- Fix critico aplicado: `ProjectZomboid64.json` ZGC -> G1GC + flags de reflection (ZGC nao emula em x86->Arm, causava SIGILL no boot).
- Portas liberadas em 3 camadas: Security List da VCN (via OCI CLI com API key), iptables interno (antes do REJECT, persistido), docker publish. UDP 16261-16262 + 8766-8767, TCP 16262-16272 (1 por slot).
- OCI CLI configurado na VM (venv /opt/oci-venv) — gestao do console por API (regras de firewall lidas/gravadas sem browser).
- Servidor online e respondingendo handshake UDP; pendencia final: GSLT token (conta que possui o jogo) para multiplayer Steam B42 sem travar em "obtendo informacoes".
- Corrigida documentacao do shape: teto Always Free do A1 Flex e hoje 4 OCPU / 24 GB (antes constava PAYG acima do teto).

**Documentos criados**
- `oracle/zomboid-server.md` (ponteiro publico sem credenciais)
- `timelog/2026-09/2026-09-15.md`
- Fora do git (host vvy): `2 Oracle/Zomboid Server/TUTORIAL-SERVIDOR.md`, `2 Oracle/API KEY/README.md`

**Documentos atualizados**
- `oracle/README.md` (servico Zomboid, correcao billing, pendencias GSLT/DuckDNS)
- `oracle/oracle-cloud.md` (billing Always Free 4/24)
- `PROXMOX_VVY.md` secao 19 (Zomboid, links, billing)
- `README.md` raiz (linha Zomboid na tabela de docs)


---

## Tarefa 2 (tarde) — Zomboid: GSLT aplicado, fix de JIT, code cache FEX

**O que foi feito**
- GSLT aplicado e validado: `steam=1` no `ProjectZomboid64.json` + `-authglslt <TOKEN>` no loop do container. Log confirma `*** Steam is enabled` sem travar em "Waiting for response"; login de jogador real (conta Familia) aceito: `Connected new client`.
- Corrigida senha de acesso do servidor no ini (rotacionada; valor real somente no tutorial local fora do git).
- Fix crash com jogador logado: SIGSEGV em frames JIT (`J c1`/`J c2`) sob FEX -> acrescentado `-Xint` (interpretador puro do JVM) no `ProjectZomboid64.json`. Servidor passou a aguentar entrada de jogador.
- Ativado code cache experimental do FEX (`EnableCodeCachingWIP`) em volume persistente `/srv/zomboid/fex-cache` (pitfalls: `FEX_APP_CACHE_LOCATION` exige barra final; Config.json via echo com aspas simples). Primeiro boot apos ativação ~10 min de compilacao do cache (janela de "servidor nao responde" normal); boots seguintes reusam.
- `SaveWorldEveryMinutes=20` no pzvvy.ini (write suave no unload de celulas).
- Criado script oficial de recriacao do container na VM: `/srv/zomboid/run-container.sh`.
- Investigado lag de carregamento de area nova: gargalo e a pilha de emulacao (FEX + JVM interpretado) + custo de gerar celula nova no tick; densidade de zumbis mantida em Normal (4 = default na escala B42); mods de threading NAO ajudam (mesmo processo emulado, WorldStreamer single-thread por design). Proximo cartucho: imagem com FEX 2609+ (disk cache nativo) em container de teste.

**Documentos atualizados**
- `oracle/zomboid-server.md` (status GSLT, -Xint, code cache FEX, script de recriacao)
- `PROXMOX_VVY.md` secao 19 (linha Zomboid: GSLT ok, fixes)
- Fora do git: `2 Oracle/Zomboid Server/TUTORIAL-SERVIDOR.md` (senhas, token, secao 7b code cache, troubleshooting)
- Skill `zomboid-server-arm` (regras 2a/2c, run-container.sh, validacao GSLT)


---

## Tarefa 3 (noite) — Zomboid: -Xint descartado, exclusões cirúrgicas de JIT

**O que foi feito**
- `-Xint` removido do `ProjectZomboid64.json`: embora tenha eliminado o SIGSEGV, derrubou o throughput do tick — geração de célula passou do timeout de 30 s do cliente (log inundado de `chunk was not generated`), jogador via quadrado carregado com arredores pretos (inviável).
- Fix definitivo: JIT ativo + `CompileCommand=exclude` apenas das classes apontadas pelos hs_err (`Bits`, `SpriteConfigManager`, `SpriteConfigManager$ObjectInfo`). Método whack-a-mole documentado: crash novo → extrair `Problematic frame` do hs_err → excluir a classe.
- Validado com jogadores reais conectados: zero SIGSEGV e zero `not generated` no boot pós-fix; entrada rápida e carregamento de áreas novas normal.
- Descartados (decisão do usuário): migrar para branch unstable (a unstable 42.19.2 é mais antiga que a stable 42.20.4 — seria downgrade com risco ao save) e rebuild da imagem com FEX 2609 (deb existe no PPA jammy e a troca é viável, mas o gargalo real era o -Xint).
- Corrigida regra da skill que recomendava -Xint como solução final.

**Documentos atualizados**
- `oracle/zomboid-server.md` (fix JIT por exclusões, alerta anti -Xint, descartados)
- `PROXMOX_VVY.md` seção 19 (linha Zomboid)
- Fora do git: `2 Oracle/Zomboid Server/TUTORIAL-SERVIDOR.md` (seções 3, 9, 10, 12)
- Skill `zomboid-server-arm` (regra 2a reescrita: exclusões > -Xint)
