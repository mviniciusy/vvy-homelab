# 2026-07-31 — Hard Freeze Investigation (continuação)

## 06:42 — Hard freeze em idle (boot -2)

- Servidor estava em **idle absoluto** (load 0.11, 0.7% CPU, sem I/O, sem swap, sem OOM)
- Último log do kernel: 04:57:43 (1h45 de silêncio antes do freeze)
- kdump vazio, sem MCE, sem EDAC errors, sem panic
- Padrão: hard freeze puro sem logs — CPU parou de executar instantaneamente
- HandBrake NÃO estava rodando — exclui VRM sob carga como causa deste freeze

## Achado: ZFS module/userspace mismatch

- Módulo kernel `zfs.ko` + `spl.ko`: **2.4.2-pve1** (em `/lib/modules/6.17.13-19-pve/`)
- Pacote userspace `zfsutils-linux`: **2.4.3-pve1**
- Mismatch ativo desde 05/Jul (apt upgrade atualizou userspace mas não módulo)
- Módulo carregava **10 threads kernel** mesmo sem pools ZFS ativos:
  - `[arc_prune]` `[arc_evict]` x5 `[arc_flush]` `[arc_reap]` `[dbuf_evict]` `[l2arc_feed]`
- Hipótese: deadlock esporádico nas threads ARC/dbuf causa hard freeze sem logs em idle

## Ação corretiva — ZFS desativado

- `systemctl stop zfs-zed` → ZED parado (segurava o módulo "in use")
- `rmmod zfs` → módulo removido do kernel
- `rmmod spl` → SPL removido
- `echo "blacklist zfs/spl" > /etc/modprobe.d/blacklist-zfs.conf` → não carrega no boot
- `systemctl disable zfs-zed zfs-share zfs-import-cache zfs-import-scan` → serviços desativados
- Threads ZFS: **0** (antes eram 10)
- Servidor não usa ZFS (root é LVM, zero pools, zero datasets)

## imagemagick/ffmpeg — descartado

- `apt install imagemagick ffmpeg` em 30/Jul 16:55 foi para skills de photo (exif/raw compress)
- Libs de userspace — não tocam kernel, não regeneraram initramfs do -19
- Coincidência temporal, não causa do freeze

## VRM — estado atual

- Vcore idle: 1040mV | 6 cores HandBrake: 992-1000mV (−40 a −48mV droop)
- VRM confirmado como causa dos freezes sob 16 cores HandBrake (carga pesada)
- Freeze das 06:42 emidle NÃO foi VRM — algo mais (ZFS mismatch é hipótese principal)

## Pendente

- [ ] Monitorar se o freeze para de acontecer com ZFS desativado (observação de 24-48h)
- [ ] Fotos da BIOS da QIYIDA X79 (usuário vai tirar depois) — verificar opções de Vcore/LLC/undervolt
- [ ] Reativar iTCO_wdt (watchdog de hardware) — servidor reinicia sozinho se travar
- [ ] Avaliar troca de placa-mãe (Machinist X99 MR9A-H + E5-2630 v4 discutida)

## Cronjob Hermes — unificação healthcheck + MCE/EDAC

- **O que foi feito:** Dois cron jobs separados (`vvy-healthcheck` + `MCE/EDAC Monitor`) unificados num único job `vvy-healthcheck` com script `vvy-healthcheck-unified.sh`
- **Script criado:** `~/.hermes/scripts/vvy-healthcheck-unified.sh` (combina healthcheck + MCE/EDAC numa só mensagem Telegram)
- **Cron jobs removidos:** `vvy-healthcheck` (job_id: a58dea13c7d6, script: healthcheck-vvy.sh) e `MCE/EDAC Monitor` (job_id: 4dc4e7d5be05, script: mce-monitor.sh)
- **Cron job criado:** `vvy-healthcheck` (job_id: f7fe30e4d0f8, script: vvy-healthcheck-unified.sh, schedule: every 120m, deliver: origin)
- **Output quando OK:** `✓ vvy saudavel — up Xh | load: N | RAM CE: N | heartbeat restarts: N`
- **Output quando alerta:** detalha alertas + MCE/EDAC + uptime + memória
- **Documentos atualizados:** timelog (este arquivo)

## Fase 1 — Mitigação de hard freeze no vvy (31/Jul/2026)

### 1.1 — Crontab limpo
- Entrada órfã `/root/monitor.sh` removida do crontab do root
- `server-watchdog.sh` mantido

### 1.2 — Headers do kernel instalados
- `proxmox-headers-6.17.13-21-pve` instalados (repo não tem -19)
- Habilita DKMS para compilação de módulos em kernels futuros

### 1.3 — ZFS desativado (mismatch 2.4.2/2.4.3)
- Módulo é precompiled dentro do pacote do kernel (não DKMS)
- Reinstalação do zfsutils-linux não recompila o módulo
- Mismatch só resolve no kernel -21
- Blacklist criado em `/etc/modprobe.d/blacklist-zfs.conf`
- Serviços ZFS desabilitados

### 1.4 — iTCO_wdt reativado permanentemente
- Blacklist removida dos kernels -19 e -21
- Hook permanente em `/etc/kernel/postinst.d/zz-unblacklist-itco.sh`
- Override `install` anterior NÃO funcionava (blacklist tem precedência)
- Initramfs do -19 regenerado
- `modprobe iTCO_wdt` confirmado

### 1.5 — VM 200 reduzida (8→4 cores)
- Isolar hipótese QEMU/KVM bug com TSC instável
- Aplica no próximo boot da VM

### Documentos atualizados
- PROXMOX_VVY.md: tabela de watchdogs, entradas de atualização 31/Jul
- docs/hermes-agent.md: script unificado, seção heartbeat externo

---

## Fase 2 — Heartbeat externo na Oracle VM (31/Jul/2026)

### O que foi feito
- Script `vvy-heartbeat.sh` criado e instalado na Oracle VM em `/opt/vvy-monitor/`
- Cron a cada 1 min via `/etc/cron.d/vvy-heartbeat`
- Monitora vvy via Tailscale (ping <TAILSCALE_VVV_IP>)
- 3 falhas consecutivas = alerta offline
- Lembrete a cada 30 min se continuar offline
- Alerta de recuperação quando vvy volta
- Bot Telegram `@hermesvvy_bot` (chat <TELEGRAM_CHAT_ID>) — testado e funcionando
- Proteções: `flock` (execução simultânea), rotação de log (5000 linhas)

### Script criado
- `/opt/vvy-monitor/vvy-heartbeat.sh` (Oracle VM)

### Documentos atualizados
- PROXMOX_VVY.md (tabela de watchdogs)
- docs/hermes-agent.md (seção heartbeat externo)
- timelog (este arquivo)

---

## Skills — Consolidação (31/Jul/2026)

### Skills absorvidas na skill `proxmox` (hub)
- `proxmox-containers` → `references/proxmox-container-vm-access.md`
- `proxmox-vm-access` → `references/proxmox-container-vm-access.md`
- `proxmox-crash-loop-diagnosis` → `references/proxmox-stability-diagnosis.md`
- `proxmox-hard-freeze-diagnosis` → `references/proxmox-stability-diagnosis.md`
- 4 skills antigas apagadas (absorbed_into=proxmox)

### SKILL.md atualizada
- Kernel: 6.17.13-6-pve → 6.17.13-19-pve
- PVE: 9.1.9 → 9.2.5
- Sub-skills: adicionadas `proxmox-disk-audit` e `proxmox-gdrive-rclone-backup`
- Nota de descoberta dinâmica + skills indiretamente relacionadas

### Documentos atualizados
- timelog (este arquivo)

## BIOS QIYIDA X79 — Configuração e alterações (31/Jul/2026)

### O que foi feito
- Documentacao completa da BIOS (32 fotos transcritas) salva em `docs/bios-vvy.md`
- Opcao C aplicada: C-states controlados via BIOS (nao mais kernel cmdline)
- `intel_idle.max_cstate=1` removido do GRUB + `update-grub`

### Alteracoes BIOS (todas aplicadas)
| Setting | Antes | Depois |
|---|---|---|
| Long duration power limit | 0 (unlimited) | 95W |
| Short duration power limit | 0 (unlimited) | 114W |
| Long duration maintained | 0 (unlimited) | 10s |
| Enable Hibernation | Enabled | Disabled |
| CPU C3 Report | Disabled | Disabled (Opcao C) |
| CPU C6 Report | Enabled | Disabled (Opcao C) |
| Package C State limit | No Limit | C0 (Opcao C) |
| QPI Link Frequency | Auto | 8.0 GT/s |
| Snoop Mode | Auto | Home Snoop |

### Reboot + verificacao
- Kernel subiu no -21 (6.17.13-21-pve) automaticamente
- iTCO_wdt carregado, VM 200 com 4 cores, Vcore 1.04V idle
- C-states: POLL/C1/C1E ativos, C3/C6 disabled (BIOS controla)
- Kernel -21 traz modulo ZFS 2.4.3 (mismatch resolvido se reativar)

### Documentos criados
- `docs/bios-vvy.md` — config completa da BIOS + alteracoes + plano de validacao

### Documentos atualizados
- `README.md` — adicionado link para bios-vvy.md
- timelog (este arquivo)

---

## Correcao do vvy-heartbeat.sh (Oracle VM) — 31/Jul/2026

### Problema
- Script detectava offline mas mensagens Telegram nao chegavam
- `curl` com `> /dev/null 2>&1` escondia falhas de envio
- Sem retry — se Telegram rejeitava (timeout/rate limit), alerta perdido

### Correcao
- Funcao `send_telegram` reescrita com:
  - 3 tentativas com delay de 2s entre cada
  - Log de cada tentativa (HTTP code)
  - Log explicito se todas as 3 falharem ("mensagem PERDIDA")
- Testado e funcionando: `Telegram OK (tentativa 1/3, HTTP 200)`

### Script modificado
- `/opt/vvy-monitor/vvy-heartbeat.sh` na Oracle VM

### Documentos atualizados
- timelog (este arquivo)
