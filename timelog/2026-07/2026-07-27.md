# Timelog — 2026-07-27

## Tarefas executadas

### Vaultwarden + Caddy na Oracle VM

**O que foi feito:**
- Instalado Docker 29.6.2 + Compose v5.3.1 na Oracle VM (Arm A1 Flex)
- Configurado DuckDNS (`<DUCKDNS_SUBDOMAIN>.duckdns.org`) com cron a cada 5 min
- Abertas portas 80/443 no iptables interno (antes do REJECT catch-all) + Security List da VCN
- Subida stack Vaultwarden + Caddy (Let's Encrypt automático)
- HTTPS validado: `https://<DUCKDNS_SUBDOMAIN>.duckdns.org` (HTTP 200)
- Signups bloqueados após criação da conta admin
- Extensão Bitwarden configurada no Vivaldi (self-hosted URL)
- App Bitwarden configurado no Android

**Documentos criados:**
- `oracle/vaultwarden/README.md` — documentação completa da stack

**Documentos atualizados:**
- `docs/backup.md` — adicionado caminho de backup do Vaultwarden no plano TeraBox

### Tailscale na Oracle VM

**O que foi feito:**
- Instalado Tailscale 1.98.9 na Oracle VM
- Conectado à tailnet (IP <TAILSCALE_ORACLE_VM_IP>)
- `--accept-routes` ativado — Oracle VM enxerga LAN 192.168.1.0/24 via subnet router do vvy
- Chave SSH da Oracle VM adicionada no vvy (authorized_keys) — SSH Oracle → vvy via Tailscale

**Documentos atualizados:**
- `docs/networking.md` — adicionado nó vvy-vnic na tabela de nós Tailscale

### WoL + ddclient

**O que foi feito:**
- Confirmado WoL ativo no vvy (nic0, MAC 22:13:5c:03:6f:51, Wake-on: g)
- Descoberto que o DDNS (NO-IP) estava desatualizado — IP público mudou e não foi atualizado
- Atualizado manualmente o NO-IP para o IP correto (<HOME_PUBLIC_IP>)
- Instalado ddclient 3.11.2 no vvy com DDNS Key do NO-IP
- ddclient ativo, verificando IP a cada 5 min
- Testado WoL: magic packet da Oracle VM chega no vvy via roteador (port forwarding porta 9 → broadcast 192.168.1.255)
- Criados scripts na Oracle VM: /opt/wol/wake-vvy.sh + /opt/wol/reboot-vvy.sh

**Documentos criados:**
- `docs/wake-on-lan.md` — doc dedicado com todos os detalhes de WoL, ddclient, scripts e troubleshooting

**Documentos atualizados:**
- `docs/networking.md` — seção 2 (ddclient) e seção 3 (WoL)
- `README.md` — adicionado Wake-on-LAN na tabela de documentação
- `PROXMOX_VVY.md` — seção 19.5 atualizada com estado atual + seção 19.6 (ddclient)

### Anti-idle Oracle Cloud

**O que foi feito:**
- Configurado cron anti-idle em /etc/cron.d/anti-idle na Oracle VM
- Ping Tailscale a cada 30 min + CPU 1 min/dia para evitar idle reclaim

### Sanitization

**O que foi feito:**
- Adicionadas entradas no sanitization-map.conf: IP Tailscale Oracle VM, token DuckDNS, credenciais DDNS Key NO-IP
