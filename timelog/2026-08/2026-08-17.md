# Timelog — 2026-08-17

## Tarefa 1: Verificacao e correcao dos scripts de backup (retention + cron)

**O que foi feito:**
- Verificacao completa do estado do backup: token OAuth do Google Drive morreu de novo (~16/08), todos uploads falhando
- Descoberta de 3 bugs:
  - Retention local: glob "vzdump-*VMID*" casava .log tambem; com keep=1 apagava o .tar.zst (backup real) e mantinha o .log
  - Cron mensal: "0 1 8-14 * 0" com Vixie cron = OR entre day-of-month e day-of-week — rodava ~10x/mes em vez de so 2o domingo
  - Logs duplicados (tee + redirect do cron)
- Criacao do helper rclone_keep.sh (CT 105) — retention remota por contagem (mantem N backups mais recentes; .log nao ocupa slot)
- Correcao dos 5 scripts de backup com:
  - Retention local e remota trocada de baseada em tempo (--min-age Nd) para contagem (keep=3)
  - Glob da retention local corrigido (so .tar.zst/.vma.zst, exclui .log; log orfao removido junto)
  - Guarda do 2o domingo adicionada ao snapshot_mensal.sh
  - Retencao remota via rclone_keep.sh (helper) em vez de rclone delete --min-age
- Atualizacao do crontab: mensal "0 1 8-14 * 0" -> "30 1 * * 0" (todo domingo 01:30 + guarda no script = 2o domingo)

**Documentos atualizados:**
- docs/backup.md (retencao, cron, paths, pitfalls)
- /root/scripts/backup/*.sh (5 scripts)
- /root/backup-manager/app/rclone_keep.sh (novo helper)
- Skill proxmox-backup (pitfalls do cron Vixie e retention bug)

**Pendentes (outra hora):**
- Token OAuth: verificar publicacao do app no Google Cloud Console + reautorizar
- Re-upload dos backups pendentes (16-17/08)
- Validacao no Drive (rclone lsd)
- sync_public.py