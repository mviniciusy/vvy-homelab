# Timelog - 2026-08-24

## Tarefa 1 - Atualização geral de pacotes (host vvy + 7 CTs)

- **O que foi feito:** Atualização completa de pacotes no host vvy (PVE) e em todos os 7 containers LXC ligados (CT 100, 101, 104, 105, 120, 160, 161). VM 200 (Docker) e CTs parados (103, 112, 199) ficaram de fora por instrução do usuário. Total de ~459 pacotes atualizados. Host não foi reiniciado (kernel -21 mantido).

- **Ordem de execução (por criticidade):**
  1. Fase A — Preparação (snapshots LVM CT 101/160/161; vzdumps diários automatizados já cobriam CT 100/104/105/120; backups de configs Nextcloud/Pihole/Zabbix em /root/.hermes/upd-logs/2026-08-24/)
  2. Fase B — Host vvy (PVE 9.2.6 → 9.2.11, 71 pacotes, ~90s)
  3. Fase C1 — CT 105 backup-manager (7 pacotes)
  4. Fase C2 — CT 100 nextcloud (21 pacotes, PHP 8.3.30 → 8.3.33)
  5. Fase C3 — CT 101 pihole (58 pacotes, bind9 9.18.47 → 9.18.49)
  6. Fase C4 — CT 120 qbittorrent (57 pacotes, em paralelo com C5)
  7. Fase C5 — CT 161 grafana (63 pacotes; necessitou `dpkg --configure -a` após interrupção de gateway)
  8. Fase C6 — CT 160 zabbix (90 pacotes, Apache 2.4.66 → 2.4.68)
  9. Fase C7 — CT 104 hermes-agent (92 pacotes, ffmpeg 5.1.8 → 5.1.9)
  10. Fase D — Limpeza (journalctl --vacuum-size=100M, find logs antigos, apt clean em CTs)
  11. Validação consolidada

- **Interrupção e recuperação:** O gateway caiu durante a sessão (entre fases C4 e C5). O upgrade do CT 161 ficou com `dpkg was interrupted`; recuperado manualmente com `dpkg --configure -a` + `apt install -f` + reaplicação de `apt full-upgrade` (já constava 0 pacotes — só faltava finalizar triggers de ca-certificates).

- **Validação final:**
  - Todos os 8 sistemas: 0 pacotes upgradable
  - 9 serviços críticos do host: active (smbd, nmbd, pveproxy, pvedaemon, ssh, tailscaled, ddclient, zabbix-agent2, postfix)
  - 7 serviços web testados via HTTP: 100 (nextcloud) 302, 101 (pihole) 302, 104 (hermes-serve) 302, 120 (qbittorrent) 200, 160 (zabbix) 200, 161 (grafana) 302, 161 (prometheus) 302
  - DNS via Pihole: resolvendo google.com → <GOOGLE_DNS_TEST_IP>
  - Watchdog iTCO_wdt: ativo
  - Uptime host: 21 min (sem reboot)

- **Limpeza pós-update:**
  - Journal do host: 278MB → 88MB (-190MB)
  - /var/log: 311MB → 121MB (-190MB)
  - APT cache nos CTs: zerado (era ~450MB)
  - /root/.cache: 1.3MB liberados
  - Snapshots LVM (CT 101/160/161) removidos após validação
  - Vzdump tmp orfão removido

- **Pontos de atenção:**
  - `lighttpd` no CT 101 nunca existiu (Pi-hole FTL serve web diretamente) — não é regressão
  - `qbittorrent` no CT 120 não tem unit systemd explícito (roda via processo direto via Docker/systemd-less) — funciona, HTTP 200
  - Backup-manager no CT 105: serviço correto é `rclone-rcd.service` (não `rclone-web` nem `rclone-cron` — esses nomes estavam errados no script de teste inicial)

- **Documentos criados:** (nenhum)
- **Documentos atualizados:**
  - `PROXMOX_VVY.md` — adicionada seção 4.1 com tabela de versões antes/depois, validação e limpeza

## Tarefa 2 - Remoção dos snapshots LVM de pre-update

- **O que foi feito:** Após ~10 minutos de validação com todos os serviços ativos, removidos os 3 snapshots LVM criados na Fase A:
  - CT 101: snap_vm-101-disk-0_pre-update-20260824-1135
  - CT 160: snap_vm-160-disk-0_pre-update-20260824-1135
  - CT 161: snap_vm-161-disk-0_pre-update-20260824-1135
- LVM pool pve/data subiu de 35% para 39% de uso (snapshots liberaram espaço)
- Backups vzdump diários de hoje continuam disponíveis em `/mnt/pve/HD-WD500GB/Dados-WD500GB/vzdump/dump/` caso rollback seja necessário (CT 100/104/105/120).

- **Documentos criados:** (nenhum)
- **Documentos atualizados:** (nenhum)
