# Timelog — 2026-08-01

## Fase 4 — Scripts de sync HDs para Google Drive (CT 105)

**Status:** Concluído
**Duração:** ~30 min

### O que foi feito

- Refatorado `sync_dados.sh` em 3 scripts separados no CT 105:
  - `sync_wd1tb.sh` — sync HD-WD-1TB (NTFS) com `--backup-dir` (lixeira)
  - `sync_wd500gb.sh` — sync HD-WD500GB (ext4) com `--backup-dir` (lixeira)
  - `clean_lixeira.sh` — `rclone delete --min-age 14d --rmdirs` (semanal DOM 04:00)
- Proteções implementadas em todos os scripts: `flock` (lock), `mountpoint` check, log em `/var/log/`, suporte `--dry-run`, `trap` para limpeza do lockfile
- Retenção da lixeira reduzida de 30 para **14 dias** (solicitação do usuário)
- Cronjobs configurados no CT 105:
  - SEG 03:00 → `sync_wd1tb.sh`
  - TER 03:00 → `sync_wd500gb.sh`
  - DOM 04:00 → `clean_lixeira.sh`
- Scripts copiados para repo privado: `/root/1 Documentação Privada/scripts/backup/`
- Dry-runs validados:
  - WD-1TB: 82.538 GiB / 39.436 arquivos
  - WD500GB: 11.390 GiB / 337 arquivos
  - clean_lixeira: "directory not found" (esperado — lixeira ainda não existe)
- Sync real WD500GB executado: **11.390 GiB / 337 arquivos** enviados ao Google Drive
- **OOM detectado**: CT 105 com 512 MiB RAM foi Killed durante o sync. Ajustado para **2048 MiB RAM + 512 MiB swap**. Sync re-rodado com sucesso.

### Lacuna de backup identificada — `/root` no host

O plano de backup atual (Fase 4) cobre apenas os HDs. Vários diretórios
em `/root` não estão incluídos. Documentado no Plano_Backup.md (seção 9.1)
e na skill (Fase 4-bis) como pendência. Pastas que precisam de backup:

- 1 Documentação Privada (3.2 MB)
- 1 Documentação GITHUB (3.1 MB)
- 1 Obsidian (32 KB)
- 2 Oracle (16 KB)
- iac (20 KB)
- logs (3.7 MB)
- Markdown (4 KB)
- scripts (112 KB)

### Permissões ajustadas

- `Tiktok/work/ds_new.py` e `fix_caption.py`: 600 → 644 (root-only → world-readable) para incluir no backup

### CT 105 — alteração de configuração

- RAM: 512 MiB → 2048 MiB
- Swap: 256 MiB → 512 MiB

### Documentos criados

- `scripts/backup/sync_wd1tb.sh` (repo privado)
- `scripts/backup/sync_wd500gb.sh` (repo privado)
- `scripts/backup/clean_lixeira.sh` (repo privado)

### Documentos atualizados

- `Plano_Backup.md` (no HD-WD500GB): 30→14 dias, seção 9.1 lacuna /root, Fase 4 marcada como concluída
- `docs/backup.md`: seção de scripts atualizada, estrutura do Drive com lixeira, fases atualizadas
- `README.md`: 3 scripts de backup adicionados na tabela de Scripts
- Skill `proxmox-gdrive-rclone-backup`: 30→14 dias, Fase 4 marcada, Fase 4-bis adicionada

### Pendências

- Sync real do HD-WD-1TB (82.5 GiB) — deixado para o cron de segunda 03:00 ou execução manual posterior
- Fase 4-bis: backup dos diretórios em `/root` (8 pastas)
- Fases 5, 6, 7 do plano de backup (snapshots, config Proxmox, restore de teste)

## Fases 5, 6 e 4-bis — Scripts de snapshot SSDs + config Proxmox + backup /root

**Status:** Scripts criados, auditados e 2 validados end-to-end
**Continuação da sessão anterior**

### O que foi feito

**Fase 5 — Snapshots SSDs (vzdump):**
- Storage `backup-dump` criado no Proxmox (dir, content=backup)
- `snapshot_hermes.sh`: vzdump CT 104 diário 02:30, retention 2 local / 7d Drive
- `snapshot_semanal.sh`: vzdump CTs 101,199,200,160,161 semanal DOM 01:00, retention 1 local / 14d Drive
- `snapshot_mensal.sh`: vzdump CTs 103,112,120 mensal 1º DOM 01:00, retention 1 local / 30d Drive
- Cronjobs configurados no host vvy

**Fase 6 — Config Proxmox:**
- `backup_proxmox_config.sh`: tar /etc/pve/ + smb.conf + crontab + iac/ diário 02:00
- Retention: 7 dias local, 30 dias no Drive
- Cronjob diário 02:00 configurado

**Fase 4-bis — Backup /root:**
- `sync_root.sh`: tar.gz de 7 pastas de /root, upload via pct exec 105
- Pastas: 1 Doc Privada, 1 Doc GITHUB, 1 Obsidian, 2 Oracle, iac, logs, scripts (Markdown removida — usuario deletou)
- Cron semanal QUA 03:00, retention 30 dias no Drive

**Auditoria dos scripts:**
- 3 bugs corrigidos: PIPESTATUS em 3 scripts de snapshot, hostname qbit-vpn → qbittorrent
- Storage backup-dump: path corrigido de /mnt/pve/HD-WD500GB/vzdump para /mnt/pve/HD-WD500GB/Dados-WD500GB/vzdump (dentro do mount point mp0 do CT 105)

**Validação end-to-end:**
- `backup_proxmox_config.sh`: OK — tar 24K criado, upload Proxmox-Config/, retention OK
- `sync_root.sh`: OK — 7 tarballs (1.95 MB total), upload Host-Root/, retention OK

**Sync WD-1TB em andamento:**
- Retomado com --transfers=8 --checkers=16 (após OOM com 4 transfers em 512MB RAM)
- CT 105 RAM: 512MB → 8GB
- ISOs excluidas (--exclude): 82.5 GiB → 72.1 GiB
- Progresso: 87% (62.4 / 72.1 GiB) em andamento

### CT 105 — alterações de configuração

- RAM: 512 MiB → 8192 MiB
- Swap: 256 MiB → 512 MiB
- Storage backup-dump: criado e ativo

### Documentos atualizados

- Plano_Backup.md: Fases 5, 6, 4-bis marcadas como CONCLUIDO, storage backup-dump adicionado
- docs/backup.md: 5 novos scripts documentados, storage backup-dump, fases atualizadas
- skill proxmox-gdrive-rclone-backup: Fases 5, 6, 4-bis marcadas, storage backup-dump, paths corrigidos

### Pendências

- Sync WD-1TB: 87% — aguardando conclusão (background)
- Fase 7: Restore de teste (restaurar CT 101 pihole do Google Drive)
- Validação dos 3 scripts de snapshot (após WD-1TB terminar — evitar I/O simultâneo)

## Correções e Ajustes — Storage, Exclusões, Auditoria e Validação

**Status:** Scripts auditados, 2 validados end-to-end, WD-1TB parcial (parado para exclusão Waves)
**Continuação da sessão**

### O que foi feito

**Storage backup-dump corrigido:**
- Path movido de /mnt/pve/HD-WD500GB/vzdump para /mnt/pve/HD-WD500GB/Dados-WD500GB/vzdump
- Motivo: o mount point mp0 do CT 105 monta Dados-WD500GB/, não a raiz do HD
- 4 scripts ajustados (backup_proxmox_config, snapshot_hermes, snapshot_semanal, snapshot_mensal)

**Exclusões do WD-1TB:**
- ISOs excluídas: --exclude=ISOs/** (Windows 10, Kali, Proxmox — ~10.4 GiB)
- WavesCentral14.17.01.23.W excluído: --exclude=WavesCentral14.17.01.23.W/** (não utilizado, ~11 GiB)
- Volume final: 59.0 GiB no Drive (antes: 70.2 GiB com Waves, 82.5 GiB com ISOs)

**Sync WD-1TB:**
- Executado até 97.4% (70.2 GiB), parado pelo usuário para exclusão Waves
- Pasta Waves purgada do Drive (~11 GiB removidos)
- Sync retomado e parado novamente — usuário vai comprimir arquivos antes

**Auditoria dos scripts de snapshot:**
- Bug 1: RC=$? → RC=${PIPESTATUS[0]} em 3 scripts (captura exit code real do vzdump, não do pipe tee)
- Bug 2: hostname qbit-vpn → qbittorrent no snapshot_mensal.sh (CT 120)
- Bug 3: comment cron corrigido no snapshot_semanal.sh

**Validação end-to-end:**
- backup_proxmox_config.sh: OK — tar 24K criado, upload Proxmox-Config/, retention OK
- sync_root.sh: OK — 7 tarballs (1.95 MB total), upload Host-Root/, retention OK
- Pasta Markdown removida do sync_root.sh (usuário deletou a pasta)

**Transfers otimizados:**
- --transfers=4 → 8, --checkers=8 → 16 (ambos scripts sync HDs)
- RAM CT 105: 512 MiB → 8192 MiB (OOM durante sync)

**Limpeza:**
- sync_dados.sh removido do CT 105 (substituído por sync_wd1tb.sh + sync_wd500gb.sh)

### Documentos atualizados
- PROXMOX_VVY.md: storage backup-dump, CT 105 RAM 8GB, cronjobs host, scripts backup
- README.md: 5 scripts novos na tabela
- docs/backup.md: já atualizado anteriormente (5 scripts, storage, fases)
- Plano_Backup.md: já atualizado (Fases 5,6,4-bis CONCLUIDO)
- skill proxmox-gdrive-rclone-backup: já atualizada (fases, storage, cronjobs)

### Pendências
- WD-1TB: sync parado em 97% — usuário vai comprimir arquivos não utilizados (7z) antes de retomar
- Fase 7: Restore de teste (restaurar CT 101 pihole do Google Drive)
- Validação dos 3 scripts de snapshot (após WD-1TB terminar)

## Correção de bugs nos scripts de backup (01/Ago/2026)

### O que foi feito

- **sync_wd1tb.sh**: alterado para sincronizar somente a pasta "1. Geral" do HD-WD-1TB (antes: HD inteiro). Removidas exclusões de ISOs/ e WavesCentral (fora do escopo). Adicionado check de existência da subpasta.
- **Bug das aspas simples no pct exec**: todos os 5 scripts de backup (snapshot_hermes.sh, snapshot_semanal.sh, snapshot_mensal.sh, backup_proxmox_config.sh, sync_root.sh) tinham aspas simples literais embutidas nos paths do rclone passados via `pct exec`. O `pct exec` (lxc-attach) re-parseia argumentos via shell e quebra paths com espaços, criando pasta `'1. vvy` na raiz do Google Drive. Corrigido com padrão `bash -c '... $1 ...' _ "$arg"`.
- **DUMP_DIR com path incompleto**: 3 scripts de snapshot usavam `/mnt/pve/HD-WD500GB/vzdump/dump` mas o path real é `/mnt/pve/HD-WD500GB/Dados-WD500GB/vzdump/dump`. Retention local não funcionava. Corrigido.
- **VM 200 backup=0**: disco scsi1 (100 GB HD-WD500GB) tinha `backup=0` configurado via `qm set 200 -scsi1 HD-WD500GB:200/vm-200-disk-0.raw,backup=0,size=100G`. Disco está vazio (só lost+found). Snapshot do VM 200 caiu de 132 GB para 6.4 GB.
- **Snapshot LV órfão CT 161**: removido `snap_vm-161-disk-0_vzdump` que ficou preso de run anterior falho.
- **Pasta `'1. vvy`**: removida do Google Drive (criada erroneamente pelos scripts com bug).
- **Scripts copiados**: 5 scripts corrigidos copiados do CT 104 para o host vvy via scp.

### Pendências

- `snapshot_mensal.sh` (CTs 103, 112, 120) não foi executado — fica para próxima sessão.
- `snapshot_semanal.sh` foi parcialmente executado (CT 101, 199, 200, 160 OK) mas interrompido antes do CT 161.
- Fase 7 (restore de teste) pendente.

### Documentos atualizados

- `docs/backup.md` — atualizado sync_wd1tb.sh, RAM CT 105, estrutura no Drive, seção de pitfalls
- `PROXMOX_VVY.md` — VM 200 vCPUs 8→4, RAM 4→6 GB, disco de dados backup=0
- Timelog (este arquivo)
