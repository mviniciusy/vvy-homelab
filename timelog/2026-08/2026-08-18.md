# Timelog — 2026-08-18

## Tarefa: Deploy hojebelem (site de eventos) na VM 200

**O que foi feito:**
- Ambiente do repo ingridslv/hojebelem (colaborador) criado na VM 200 via Docker Compose em /opt/hojebelem
- Postgres 16 (porta 5432) + app Next.js 16 (porta 3000) — http://<DOCKER_VM_IP>:3000 (LAN e via subnet routing do vvy no Tailscale)
- Correções locais necessárias para o build (repo quebrado):
  - package-lock.json regenerado (lock desatualizado — faltavam @emnapi/*)
  - src/app/layout.tsx corrigido (LayoutProps inexistente -> Readonly<{children: ReactNode}>)
  - Dockerfile.local com ARG DATABASE_URL (build do repo original falha sem o banco)
  - docker-compose.override.yml: restart unless-stopped, volume uploads, AUTH_TRUST_HOST=true
- Seed aplicado (34 categorias, 6 eventos, admin)

**Documentos atualizados:**
- PROXMOX_VVY.md (secao 13: vCPUs 8, nota 18/Ago)
- docs/docker-vm.md (secao 3 hojebelem)

**Pendentes:**
- Branch fix/local-build com as correcoes — decidir PR para a Ingrid
