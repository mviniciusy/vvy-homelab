# Timelog — 2026-08-04

## Tarefa 1: Atualização do Pi-hole + adição de lista HaGeZi Pro

### Contexto

Verificação de rotina do container Pi-hole (CT 101, <PIHOLE_IP>). Solicitado
pelo usuário: revisar se algo poderia ser melhorado no bloqueio de anúncios
e se faria sentido instalar outro bloqueador junto.

### O que foi diagnosticado

| Item | Estado | Observação |
|---|---|---|
| Serviço `pihole-FTL` | active | 3 dias de uptime, 167 MB RAM, sem erros no log |
| Portas 53/80/443 | Listening (IPv4+IPv6) | DNS, HTTP, HTTPS OK |
| `dig @127.0.0.1 cloudflare.com` | `<CLOUDFLARE_PUBLIC_IP>` | Resolução OK |
| `dig @127.0.0.1 doubleclick.net` | `0.0.0.0` | Bloqueado (gravity funcionando) |
| Versões | Core v6.3 / Web v6.4 / FTL v6.4.1 | **3 releases atrás** do latest (Core v6.4.3 / Web v6.6 / FTL v6.7) |
| Adlists | 1 (StevenBlack hosts) | 99.276 domínios na gravity |
| Config | `/etc/pihole/pihole.toml` (TOML, v6) | DNSSEC on, 5 upstreams, cache 10000, listeningMode=ALL |
| `sqlite3` | **não instalado** | Necessário p/ gerenciar adlists via CLI na v6 |

### Decisão do usuário

- **A** — Atualizar Pi-hole (Core + Web + FTL)
- **D** — Adicionar lista HaGeZi Pro (~280k domínios)

### Correções aplicadas

#### A — Atualização (pihole -up)

- Snapshot de segurança criado antes: `pct snapshot 101 pre-upgrade-2026-08-04`
  (removido a pedido do usuário após validação)
- `pihole -up` subiu os 3 componentes em uma única passada (all-or-nothing):
  - Core v6.3 → v6.4.3
  - Web v6.4 → v6.6
  - FTL v6.4.1 → v6.7
- Serviço reiniciou (PID mudou), DNS voltou em <2s
- Gravity recompilada atomicamente (swap), 99.276 domínios preservados

#### D — Adição da lista HaGeZi Pro

**Tentativa 1 (falhou):** tentada URL `dnsmasq/pro.txt` — formato dnsmasq
`sintaxe local=/domain/` não é parseada pelo Pi-hole (0 domínios válidos,
218.620 linhas ignoradas como "non-domain entries").

**Tentativa 2 (sucesso):** URL `adblock/pro.txt` — formato Adblock Plus
(`[Adblock Plus]` no cabeçalho), suportado nativamente pelo Pi-hole v6
como "ABP-style domains". 218.620 domínios parseados e aplicados.

Procedimento aplicado:

1. `apt-get install -y --no-install-recommends sqlite3` (no CT 101)
2. `INSERT` na tabela `adlist` do `gravity.db` com a URL ABP
3. `INSERT` na tabela `adlist_by_group` (linkar ao grupo Default id=0)
4. `pihole -g` (updateGravity) — rebuild atômico da gravity

### Resultado final

| Métrica | Antes | Depois |
|---|---|---|
| Core / Web / FTL | v6.3 / v6.4 / v6.4.1 | v6.4.3 / v6.6 / **v6.7** |
| Adlists | 1 | 2 |
| Domínios na gravity | 99.276 | **317.896** (+220%) |
| `analytics.google.com` | bloqueado | bloqueado |
| `vortex.data.microsoft.com` (telemetria MS) | **passava** | **bloqueado** ✓ |
| RAM pihole-FTL | 167 MB | ~180 MB |
| Latência DNS | sem mudança | sem mudança |

### Validação

```
dig @127.0.0.1 cloudflare.com           → <CLOUDFLARE_PUBLIC_IP> (resolvido)
dig @127.0.0.1 doubleclick.net          → 0.0.0.0 (bloqueado)
dig @127.0.0.1 analytics.google.com     → 0.0.0.0 (bloqueado)
dig @127.0.0.1 vortex.data.microsoft.com → 0.0.0.0 (bloqueado, novo)
dig @127.0.0.1 github.com               → <GITHUB_PUBLIC_IP> (resolvido)
pihole status                           → [✓] blocking enabled
```

### Documentos atualizados

- `/root/.hermes/skills/devops/proxmox-networking/SKILL.md`
  - Seção Pi-hole reescrita: versões v6.4.3/v6.7/v6.6, tabela de adlists,
    procedimento CLI via sqlite3, pitfall do formato HaGeZi (dnsmasq vs adblock)
- `/root/.hermes/skills/proxmox/SKILL.md`
  - Linha CT 101 da tabela de containers atualizada com versões + gravity count
- CT 101: `sqlite3` instalado (pacote Debian, <100KB)

### Lições

1. **Pitfall HaGeZi — formato importa**: A HaGeZi distribui em vários
   formatos. Para Pi-hole, `dnsmasq/pro.txt` **não funciona** — usa sintaxe
   `local=/domain/` que o Pi-hole ignora (0 domínios parseados). O caminho
   **correto** é `adblock/pro.txt` (formato Adblock Plus), suportado pelo
   Pi-hole v6 como "ABP-style domains". Sempre validar com `pihole -g` e
   checar "Parsed N exact domains and M ABP-style domains" — se ambos
   forem 0, o formato está errado.

2. **`pct exec` não expõe `/usr/local/bin`**: o binário `pihole` retorna
   "No such file or directory" via `pct exec 101 -- pihole ...`. Usar
   sempre caminho absoluto: `pct exec 101 -- /usr/local/bin/pihole ...`.

3. **`pihole -up` é all-or-nothing**: sobe Core+Web+FTL juntos. Não há
   flag `--check` que execute de verdade (só sai antes de fazer). Se você
   quisesse atualizar só o FTL, teria que usar `pihole checkout ftl v6.7`.

4. **Adicionar lista vs adicionar bloqueador**: a pergunta original do
   usuário foi "tem como instalar outro bloqueador junto a ele? isso
   criaria latência ou perda de rede?". Resposta: não precisa — Pi-hole é
   DNS central e adicionar mais listas à gravity existente é o caminho
   correto. Um segundo bloqueador DNS (AdGuard Home, NextDNS) em paralelo
   não faria sentido (clientes teriam que ser configurados pra consultá-lo
   manualmente). O custo de mais listas é mínimo: FTL saltou de 167→180MB
   de RAM. updateGravity é atômico (recompila em paralelo, swap no final,
   DNS para <1s).

---

## Tarefa 2: Correção de falhas de backup do snapshot_hermes + backup_proxmox_config

### Contexto

Verificação de rotina solicitada pelo usuário ("verifique se o backup está
rodando"). Diagnosticadas 2 falhas em scripts de backup na madrugada de 04/08.

### O que foi diagnosticado

| # | Script | Horário | Sintoma | Causa-raiz |
|---|---|---|---|---|
| 1 | `snapshot_hermes.sh` | 04/08 02:20 | Abortou em "Storage backup-dump não encontrado após 5 tentativas" | `pvesm status` sob concorrência pmxcfs às 02:20 (single-node, corosync/pve-ha-crm inactive); 5×15s (75s) não bastaram |
| 2 | `backup_proxmox_config.sh` | 04/08 02:00 | Exit 5 ("Falha no upload rclone"); tar.gz copiado localmente mas não subiu | rclone copy sem `--verbose`/stderr; erro real perdido — reintento manual funcionou em 4s |

### Correções aplicadas

**snapshot_hermes.sh** (host vvy: `/root/scripts/backup/`)

- **Retry aumentado**: 5×15s (75s) → **8×20s (160s)** para tolerar concorrência
  de pmxcfs por mais tempo
- **Ordem do check invertida**: `pvesm list backup-dump` agora é primário (mais
  resiliente que `pvesm status|awk|grep -qx` sob lock pmxcfs); `pvesm status`
  mantido como fallback
- **Log atualizado**: mensagens `Tentativa N/8` e `Aguardando 20s`
- **Cron movido**: 02:20 → **02:45** (45 min após `backup_proxmox_config.sh`
  às 02:00, deixando folga para upload rclone terminar)
- **rclone upload + rclone delete**: adicionado `--verbose` para capturar
  stderr real nos logs

**backup_proxmox_config.sh** (host vvy: `/root/scripts/backup/`)

- **rclone copy**: `--verbose 2>&1 | tee -a "$LOG"` — antes stderr era descartado,
  erro real perdido ("Falha no upload rclone" sem detalhe)
- **rclone delete (retention remota)**: idem, `--verbose 2>&1 | tee -a "$LOG"`

**Sanitization-map** (`/root/1 Documentação Privada/sanitization-map.conf`)

- Adicionados 2 IPs públicos DNS (`<CLOUDFLARE_PUBLIC_IP>`, `<GITHUB_PUBLIC_IP>`) — são
  resultados de `dig` no timelog, não sensíveis, mas `sync_public.py` flaggeava
  como "vazamento". Mapeados para `<CLOUDFLARE_PUBLIC_IP>`, `<GITHUB_PUBLIC_IP>`.

### Reexecução manual (snapshot_hermes.sh)

| Etapa | Resultado | Evidência |
|---|---|---|
| Storage check | ✓ OK na 1ª tentativa (script antigo) | log 21:50:11 |
| vzdump 104 | ✓ 1m32s, 3.77 GB | `vzdump-lxc-104-2026_08_04-20_15_33.tar.zst` |
| Upload rclone | ✓ 7m (3.77 GB → Drive) | confirmado no Drive: 4.05 GB 20:17:04 |
| Retention local | ✓ removeu 2 arquivos antigos (03/08 21:43) | `removido: vzdump-lxc-104-2026_08_03-...` |
| Retention remota | ✓ deletou 7+dias no Drive | log 20:24:01 |

### Upload do backup_proxmox_config de 04/08 (refeito manualmente)

- Tar.gz local de 04/08 02:00 (24 KB) não tinha subido (script falhou no upload)
- Upload manual via `rclone copy` concluído em 4s: `etc-pve-2026-08-04.tar.gz`
- Confirmado no Drive: 24007 B, 02:00:01

### Documentos atualizados

- `/root/scripts/backup/snapshot_hermes.sh` — retry 8×20s + pvesm list primary + --verbose
- `/root/scripts/backup/backup_proxmox_config.sh` — rclone --verbose + stderr capturado
- `crontab` do root no vvy — snapshot_hermes 02:20 → 02:45
- `/root/1 Documentação Privada/scripts/backup/` — cópias sincronizadas (diff zero)
- `/root/1 Documentação Privada/sanitization-map.conf` — 2 IPs públicos DNS adicionados
- Backups `.bak.20260804-2` dos 2 scripts no host

### Git

- Repo privado (`vvy-homelab-priv`): 2 commits (7643060, 11ab93a) + push
- Repo público (`vvy-homelab`): 2 commits via `sync_public.py` + push, sem vazamentos

### Próximas execuções automáticas (após correção)

| Quando | Script |
|---|---|
| Qui 07/08 02:00 | `backup_proxmox_config.sh` |
| Qui 07/08 **02:45** | `snapshot_hermes.sh` ← horário novo |

### Lições

1. **Retry de storage: aumentar não é só estender** —
   saltar de 3×10s → 5×15s não resolveu (04/08 falhou novamente). 8×20s (160s)
   cobre janela de contenção pmxcfs mais longa. Cron deslocado (02:20 → 02:45)
   reduz a concorrência inicial em vez de só tolerá-la — ambas as medidas
   juntas são mais robustas que só uma.

2. **rclone sem --verbose oculta erro real** —
   quando o `pct exec 105 -- bash -c rclone copy  ` falha, o stderr
   do rclone é descartado; o script só loga "Falha no upload rclone (exit 5)"
   sem dizer por quê. `--verbose 2>&1 | tee -a "$LOG"` expõe o erro real (rate
   limit, token expirado, transient network) no log.

3. **pvesm list vs pvesm status sob lock** —
   `pvesm list <storage>` é mais direto que `pvesm status | awk | grep -qx`
   porque (a) não precisa de pipeline awk+grep, (b) acessa o sqlite do pmxcfs
   de forma mais isolada, (c) retorna erro mais cedo e limpo. Inverter a ordem
   no `if` torna `pvesm list` o primário — preferencial sob contenção.

4. **Falsos positivos no sync_public.py** —
   IPs públicos resolvidos por `dig` (Cloudflare, GitHub) são flaggeados
   como "vazamento". Solução: adicionar ao `sanitization-map.conf` como
   placeholders. Não são sensíveis (são IPs de serviços públicos), mas
   o verificador é conservador por design — melhor prevenir.
